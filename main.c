#include "cell.h"
#include "list.h"

int main() {
    Level_list *mylist = createEmptyLevel_list(2);
    isEmptyList(mylist);
    //printListLevel(mylist,2)
    //Cell *cell = createCell(2,2);
    //printCell(cell);
//    insertCellAtHead(mylist,cell);
//    //addCellAtHead(mylist,cell);
//    printListLevel(mylist, 0);
    return 0;
}
